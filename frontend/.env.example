# Yerel geliştirmede backend ayrı bir portta (Vite dev sunucusundan farklı
# origin) çalıştığı için bunu açıkça belirtin. Production build'de (HF
# Spaces) boş bırakılır - frontend backend ile aynı origin'den servis
# edilir, varsayılan zaten bunu yapar.
VITE_API_BASE=http://localhost:8000
