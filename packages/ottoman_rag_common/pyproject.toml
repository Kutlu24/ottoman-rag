[project]
name = "ottoman-rag-common"
version = "0.1.0"
description = "Osmanlica RAG projesi icin ortak provenance/HTR semalari"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ottoman_rag_common"]
